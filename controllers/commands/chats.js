module.exports = (server) => {

    // server.on('message', (event) => {

    //     db.run(`INSERT INTO chats(player, message, timestamp) VALUES (?, ?, ?)`, [event.player, event.message, event.timestamp], function (err) {
    //         if (err) {
    //             return console.log(err.message);
    //         }
    //         console.log(`${event.player} chat stored`);
    //     });

    // });

};